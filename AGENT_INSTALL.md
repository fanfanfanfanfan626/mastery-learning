# Compatibility installation entrypoint

`AI_INSTALL.md` is the single host-routing contract for Mastery Tutor. Read and follow it first.

This filename remains in the 0.5.0 migration release so links created during the earlier
`mastery-learning` preview do not become ambiguous. It must not contain a second installation
procedure.
